# CN-ES Interpreter v0.10.0-dev.086

`v0.10.0-dev.086` 基于 `v0.9.0-dev.085` 冻结版本，重点增加本地授权与防破解加固，同时保持原有同传、单向翻译、多端字幕同步和会议纪要业务逻辑不变。

## 下载信息

- 推荐下载：`CN-ES-Interpreter-v0.10.0-dev.086-macos-arm64.dmg`
- 备用下载：`CN-ES-Interpreter-v0.10.0-dev.086-macos-arm64.zip`
- 平台：macOS Apple Silicon / arm64
- DMG 文件大小：约 55 MB
- DMG SHA-256：`175f3757ce602e383dc354154d72c2c46649526650862d30e52d2f82c58922fc`
- ZIP SHA-256：`680aee274166a6c422d98884675e99298f915e3827f40b6c4d8a4fa7f2aba917`

## 客户介绍图

本发布页同步附带 5 张客户介绍图，覆盖产品定位、3 步使用路径、同传模式、单向翻译和会议同传工作台差异化说明。图片也已提交到仓库 `assets/` 目录，方便在 README 中直接预览。

## 本版本重点

- Rust/Tauri 启动 Python sidecar 时注入一次性启动令牌。
- Python sidecar 在启动阶段校验启动令牌，减少单独运行后端绕过入口的风险。
- 本地授权文件增加签名校验，降低直接修改授权文件的绕过风险。
- 试用记录增加防重置哨兵文件，删除主授权文件不会恢复完整试用时长。
- 保留 WebSocket `start` 授权拦截，前端 UI 只作为展示和入口。

## 产品说明

CN-ES Interpreter 把跨语言会议中的常用动作整合在同一个工作台里：实时识别原文、生成目标语言字幕、按需播放目标语言语音，并把字幕同步给手机、Web 页面和大屏显示端。观众端不需要主动录音，也可以作为旁听字幕或投屏字幕使用。

## 发布前检查

该成品包已完成发布前安全扫描：

- 路径扫描：PASS
- 内容扫描：PASS
- 已知凭据扫描：PASS
- 扫描完整性：PASS
- 扫描文件数：219
- 扫描路径数：293

未发现 `.env`、常见 API Key、Token、私钥或硬编码凭据。

DMG 由已扫描通过的 ZIP/App 产物生成，并已通过 `hdiutil verify` 完整性校验。
